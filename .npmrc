# Contournement certificat TLS sur certains réseaux Windows (proxy/antivirus).
# À retirer une fois le problème SSL résolu sur la machine.
# Voir README.md section "Dépannage npm".
strict-ssl=false
fetch-retries=2
fetch-retry-mintimeout=2000
fetch-retry-maxtimeout=10000
